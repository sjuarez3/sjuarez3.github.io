// Sets up shortcuts to Firebase features and initiate firebase auth.
program-planning-tool.prototype.initFirebase = function() {
  // Shortcuts to Firebase SDK features.
  this.database = firebase.database();
};